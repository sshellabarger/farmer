rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Cloud Functions (server-side).
    // No direct client reads/writes from the browser.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
